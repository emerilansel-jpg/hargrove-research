---
interface Props { icon: string; label: string }
const { icon, label } = Astro.props;
---
<span class="trust-badge">
  <span class="trust-badge__icon" set:html={icon} />
  <span>{label}</span>
</span>
